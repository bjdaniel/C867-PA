#ifndef DEGREE_H
#define DEGREE_H

// Enum of degree programs, including an UNDEFINED enum if an input record has a degree program
// not defined within this enum.
enum DegreeProgram {
	SECURITY,
	NETWORK,
	SOFTWARE,
	UNDEFINED
};

#endif // !DEGREE_H